using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SideQuestion : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        /* 1. The method name is TakeDamage. 2. The method returns an int.  3.  There is a damageAmount parameter, making one parameter. 4. The code depicts the int health being equal to 100 and the method return health - damageAmount. */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
